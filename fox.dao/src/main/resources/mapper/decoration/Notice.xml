<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.minutch.fox.dao.decoration.NoticeDao">

    <resultMap id = "BaseResultMap" type = "com.minutch.fox.entity.decoration.Notice"
               extends = "COMMON.BASE_RESULT_MAP">
        <result column = "nitice_type" property = "niticeType" />
        <result column = "notice_message" property = "noticeMessage" />
        <result column = "url" property = "url" />
        <result column = "remark" property = "remark" />
    </resultMap>

    <sql id = "BaseWhereClause">
        <where>
            <trim prefixOverrides = "and">
                <include refid = "COMMON.BASE_WHERE_CLAUSE" />
                <if test = "niticeType != null">
                    and nitice_type = #{niticeType}
                </if>
                <if test = "noticeMessage != null">
                    and notice_message = #{noticeMessage}
                </if>
                <if test = "url != null">
                    and url = #{url}
                </if>
                <if test = "remark != null">
                    and remark = #{remark}
                </if>
            </trim>
        </where>
    </sql>

    <sql id = "BaseUpdateSet">
        <set>
            <include refid = "COMMON.BASE_UPDATE_SET" />
            <if test = "niticeType != null">
                nitice_type = #{niticeType},
            </if>
            <if test = "noticeMessage != null">
                notice_message = #{noticeMessage},
            </if>
            <if test = "url != null">
                url = #{url},
            </if>
            <if test = "remark != null">
                remark = #{remark}
            </if>
        </set>
    </sql>

    <!-- 查询总数 -->
    <select id = "selectCount" resultType = "java.lang.Integer">
        select count(id)
        from decoration_notice
        <include refid = "BaseWhereClause" />
    </select>

    <!-- 查询 -->
    <select id = "select" resultMap = "BaseResultMap">
        select
        <include refid = "COMMON.ALL_COLUMN_LIST" />
        from decoration_notice
        <include refid = "BaseWhereClause" />
        <include refid = "COMMON.ORDER_BY" />
        <include refid = "COMMON.LIMIT" />
    </select>

    <!-- 根据ID查询 -->
    <select id = "selectById" resultMap = "BaseResultMap">
        select
        <include refid = "COMMON.ALL_COLUMN_LIST" />
        from decoration_notice
        where id = #{id}
    </select>

    <!-- 根据IDS批量删除 -->
    <update id = "deleteByIds">
        update decoration_notice
        set is_deleted='Y'
        where id in
        (
        <foreach collection = "array" index = "index" item = "tag" separator = ",">
            #{tag}
        </foreach>
        )
    </update>

    <!-- 根据ID删除 -->
    <update id = "deleteById">
        update decoration_notice
        set is_deleted='Y'
        where id=#{id}
    </update>

    <!-- 删除 -->
    <delete id = "delete">
        update decoration_notice
        set is_deleted='Y'
        <include refid = "BaseWhereClause" />
    </delete>

    <!-- 添加   -->
    <insert id = "insert" useGeneratedKeys = "true" keyProperty = "id">
        insert into decoration_notice (
        <include refid = "COMMON.BASE_INSERT_COLUMN" />
        nitice_type,
        notice_message,
        url,
        remark
        )
        values (
        <include refid = "COMMON.BASE_INSERT_VALUE" />
        #{niticeType},
        #{noticeMessage},
        #{url},
        #{remark}
        )
    </insert>

    <!-- 通过ID更新 -->
    <update id = "updateById">
        update decoration_notice
        <include refid = "BaseUpdateSet" />
        where id = #{id}
    </update>



    <!-- 查询最新的通知 -->
    <select id = "queryNewNotice" resultMap = "BaseResultMap">
        select *
        from decoration_notice
        where is_deleted = 'N'
        order by id desc
        limit 1;
    </select>

</mapper>
